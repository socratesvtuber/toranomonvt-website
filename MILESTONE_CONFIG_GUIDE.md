# キリ番設定ファイル 使い方ガイド

## 概要

キリ番カウンターの設定は、`milestone-config.json` ファイルで管理されています。このファイルを変更することで、JavaScript のコードを触らずにキリ番の達成条件や再生する音声ファイルを変更できます。

## 設定ファイルの場所

```
/milestone-config.json
```

## 設定方法

### 基本構造

```json
{
  "milestoneInterval": 100,
  "specialMilestones": [
    {
      "count": 1,
      "voice": "audio/voice/音声ファイル名.m4a",
      "message": "表示メッセージ"
    }
  ]
}
```

### パラメータの説明

| パラメータ | 説明 | デフォルト値 |
|-----------|------|-------------|
| `milestoneInterval` | 通常のキリ番判定の間隔（例：100 なら 100, 200, 300...） | `100` |
| `specialMilestones` | 特別キリ番の設定配列 | - |
| `specialMilestones[].count` | キリ番のカウント数 | - |
| `specialMilestones[].voice` | 再生する音声ファイルのパス | - |
| `specialMilestones[].message` | 表示するメッセージ | - |

### 設定例

```json
{
  "milestoneInterval": 100,
  "specialMilestones": [
    {
      "count": 1,
      "voice": "audio/voice/スーパーファン感謝_20251111_1.m4a",
      "message": "🎉 初訪問ありがとうございます！"
    },
    {
      "count": 100,
      "voice": "audio/voice/kaka ちゃんセリフ 1.m4a",
      "message": "🎊 100 回記念！ありがとうございます！"
    },
    {
      "count": 500,
      "voice": "audio/voice/夜叉さんセリフ 1.m4a",
      "message": "🎊 500 回記念！ありがとうございます！"
    },
    {
      "count": 1000,
      "voice": "audio/voice/スーパーファン感謝_20251111_1.m4a",
      "message": "🎊 1000 回記念！ありがとうございます！"
    },
    {
      "count": 5000,
      "voice": "audio/voice/kaka ちゃんセリフ 1.m4a",
      "message": "🎊 5000 回記念！ありがとうございます！"
    },
    {
      "count": 10000,
      "voice": "audio/voice/夜叉さんセリフ 1.m4a",
      "message": "🎊 10000 回記念！ありがとうございます！"
    }
  ]
}
```

## 音声ファイルの追加方法

1. 音声ファイルを `audio/voice/` ディレクトリに配置
2. `milestone-config.json` の `specialMilestones` 配列に新しいエントリを追加
3. `count` に達成したいカウント数、`voice` に音声ファイルのパス、`message` に表示メッセージを指定

## 注意点

- JSON 形式の構文を正しく保ってください（カンマ、クォート、ブラケットなど）
- 音声ファイルのパスは、実際のファイルの場所と一致している必要があります
- `milestone-config.json` はブラウザにキャッシュされるため、変更後はキャッシュをクリアする必要がある場合があります
- 設定ファイルの読み込みに失敗した場合は、デフォルト値が使用されます

## 動作

1. ページ読み込み時に `milestone-config.json` が自動的に読み込まれます
2. カウンターが特別キリ番に達すると、対応する音声ファイルが再生されます
3. 音声ファイルはブラウザのオートプレイポリシーに従い、ユーザーのインタラクションがない場合は再生が待機されます

## トラブルシューティング

### 設定が反映されない場合
- ブラウザのキャッシュをクリアしてください
- 開発者ツールのネットワークタブで `milestone-config.json` が正常に読み込まれているか確認してください

### 音声ファイルが見つからない
- ファイルパスが正しいか確認してください
- 音声ファイルの形式がブラウザでサポートされているか確認してください（.m4a, .mp3 など）

### 音声が再生されない
- ブラウザのオートプレイ制限により、ユーザーのインタラクション（クリックなど）が必要な場合があります
- ブラウザの音量設定を確認してください
