// Audio Voice Player - Disabled
// Page transition audio playback has been disabled per user request.
// Voice playback is now only triggered by user interaction on member profile pages.

// Placeholder to prevent errors if referenced
window.stopCurrentAudio = function() {
  // No-op: Audio playback on page transition is disabled
};

// No automatic initialization
// Voice playback is handled individually on member profile pages
